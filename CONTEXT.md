# CONTEXT.md — music-license

> 이 문서는 **무엇을, 왜** 만드는지를 담는다. (어떻게는 → [CLAUDE.md](./CLAUDE.md))
> **살아있는 문서**다. 방향이 바뀌면 본문을 고치고, 그 결정을 아래 **결정 로그**에 날짜와 함께 남긴다.

---

## 한 줄 정의
NetEase Cloud Music을 위한 **아티스트 발굴·평가·라이선스 인텔리전스 도구**.

## 회사 맥락
- **소속**: NetEase Cloud Music (网易云音乐) — **2026-08 입사(신규)**
- **하는 일**: A&R — 회사의 아티스트 풀 확장
- **최종 목표**: 더 좋은 아티스트(신인·기존 무관)를 발굴해 회사와 **라이선스 계약**을 맺게 한다.
- **사내 협업 도구**: **网易POPO** (메신저·문서·캘린더·결재 통합). 자동화 접점·제약은 → [docs/popo-automation-context.md](./docs/popo-automation-context.md)

## 우리가 푸는 문제
공개 스트리밍/소셜 + 외부 차트 데이터를 모아 아티스트를 분석하고, 일관된 기준으로 점수화해 **"라이선스할 만한 후보"를 추려내고 그 근거를 설명**한다. 사람이 일일이 손으로 뒤지는 발굴을 데이터로 거든다.

## "더 좋은 아티스트"의 기준 — A&R 논지 (4축)
점수는 단일 숫자가 아니다. 4개 축을 **각각 근거와 함께** 산출하고, **맥락 종합 코멘트**로 "왜 이 아티스트인가"의 서사를 만든다.

1. **성장세 / 모멘텀** — 최근 재생·팔로워·저장의 증가 속도. 이미 큰 스타보다 *"뜨는 중"*을 우선.
2. **참여 · 팬 충성도** — 재생 대비 좋아요·공유·댓글·재청취 등 *"관계의 깊이"*. 단순 규모가 아님.
3. **지역 / 장르 적합성** — NetEase 이용층(중국·아시아 시장)과의 장르·언어·정서 적합도. 플랫폼 매칭이 잘 될 아티스트.
4. **계약 가능성 / 공백** — 독점·메이저 계약 공백, 라이선스 여지, 접촉 난이도.

> ②·③처럼 맥락이 핵심인 축은 단순 수치로 환원하지 않는다. 종합 코멘트가 최종 판단의 무게중심.
> 위 4축의 **운영 기준**은 바로 아래 「아티스트 픽업 기준」 6항목으로 구체화된다.

## 아티스트 픽업 기준 (정본)
> 원래 출처는 옵시디언 "아티스트픽업" 노트였으나 **이제 이 문서가 정본**이다. 발굴·점수화는 항상 아래 기준을 따른다. (운영 구현 = `artist-pickup` 스킬)

**공통 규칙**
- **전부 소프트 가중 · 하드 탈락선 없음** — 어떤 기준도 후보를 자동 탈락시키지 않는다. (결정 #002)
- **타깃 = 서양권 우선** — 중화권은 이미 중국 시장을 섭렵 → 화이트스페이스는 *서양 힙합/R&B/POP을 중국에 들여오는 것*. (결정 #003)
- **나이는 점수 항목이 아니라 저장·표시 항목** (타겟 오디언스 산정용).
- **정규화**: 노트가 명시 기준을 준 항목(IG 50만↑, 장르 우선)은 **절대 앵커**, 연속 수치(스트리밍·팔로워·카탈로그)는 **풀 내 상대**.

**점수 기준 (4축)**

| # | 기준 | 비중 | 원문 정의 → 읽는 법 |
|---|------|:---:|------|
| 1 | **Spotify + 주요 글로벌 플랫폼 스트리밍** | 30% | 공개 스트리밍 실적. Spotify에 더해 Apple Music·YouTube Music 등 다른 글로벌 플랫폼 수치가 있으면 함께 반영. **정확성 1순위 데이터** — 가장 정확해야 한다. |
| 2 | **장르** | 30% | **힙합·R&B·POP 우선**(가중 높음). 나머지 장르도 배제 아님, 우선순위만 낮음. |
| 3 | **SNS 팔로워** (IG 중심) | 20% | IG 50만↑ = 좋은 등급(절대 앵커). 그 아래는 규모 + 풀 내 상대. IG 없으면 YT 대체. |
| 4 | **카탈로그 규모 + 시장 반응** | 20% | 발매 곡·앨범 수 + "지금까지 어떤 결과물을 냈고 반응이 어땠는지" 분석. |
| — | (나이) | 점수 X | 저장·표시 항목. 타겟 오디언스 산정용. |

> **비중 근거** (결정 #006): 스트리밍·장르 = 30%, 팔로워·카탈로그 = 20%. 기존 6축의 상대 비율(높음 21% : 중간 14%)을 4축으로 재정규화. "음악·스트리밍·세일즈 데이터가 가장 중요하고 정확해야 한다"는 원칙 유지.

**비점수 영역 (의견만, 점수 X)** — 라이선스 판단에 중요하지만 한 줄 숫자로 환원하면 왜곡되는 정성 영역. 각 아티스트마다 한 줄 의견 + 근거를 달아 스코어카드에 "참고 의견(점수 미반영)"으로 표시. (결정 #006)

| 영역 | 의견에 담을 것 |
|------|------|
| **인디 vs 메이저** | 소속과 직접 계약 용이성. 메이저 = 레이블 경유 필요, 인디 = 직접 계약 가능 → 인디가 유리. |
| **국적 · 중국 활동 가능성** | 중국 활동 이력·검열/정치 리스크. 활동 제한되면 프로모션 자체가 불가. 서양권 타깃 맥락에선 "중국에 들여올 수 있나"로 읽는다. |

## 데이터 전략
- **소스**: 공개 스트리밍/소셜 플랫폼(Spotify, YouTube, SoundCloud, TikTok 등) + 외부 차트·데이터 벤더(Chartmetric 등).
- **획득 원칙**: *수단·방법을 가리지 않는다.* 파이썬 API 클라이언트·스크레이퍼·외부 깃헙 스킬·MCP·수동 입력 등 무엇이든, 데이터를 **코어 모델로 정규화**만 하면 어댑터가 된다.
  - 단, 항상 **합법·각 플랫폼 ToS·저작권·개인정보 범위 내**에서. (→ [CLAUDE.md](./CLAUDE.md) 원칙 2)

## 구조 개념 *(개념만 — 아직 코드 없음)*
- **코어 (안정)**: 데이터 모델 + 4축 스코어링. 프로젝트의 두뇌.
- **획득 / 어댑터 (기회주의적)**: 소스별 수집 → 코어 모델로 정규화.
- **딜리버리 스킬 (온디맨드)**: 점수화 데이터 → 노션/엑셀/메일/리포트/대시보드. 맥락 따라 그때그때 스킬로 제작.

## 지금은 안 한다 *(스코프 경계)*
- 폴더 골격·코드 스캐폴딩·웹 대시보드·DB → 전부 **필요해질 때** 추가.
- 현재 단계: **CLAUDE.md / CONTEXT.md로 방향만 세우고 점진적으로 키운다.**

## 용어집
- **어댑터(adapter)** — 한 소스에서 데이터를 가져와 코어 모델로 정규화하는 모듈/수단.
- **딜리버리 스킬** — 점수화 결과를 특정 형식으로 내보내는 Claude Code 스킬.
- **4축 점수** — 성장세 · 충성도 · 적합성 · 계약여지 (개념 논지).
- **픽업 기준 (4점수축 + 2의견)** — 점수: 스트리밍 · 장르 · 팔로워 · 카탈로그. 비점수 의견: 인디/메이저 · 국적/중국활동. 4축 논지의 *운영* 기준 (→ 「아티스트 픽업 기준」 섹션). (결정 #006으로 6항목 → 4점수+2의견 개편)
- **코어(core)** — 수집·분석·점수화의 안정적 중심 로직.

---

## 결정 로그
형식: `#번호 (YYYY-MM-DD) — 결정 / 이유`

### #001 (2026-06-18) — 프로젝트 출범 & 기본 방향 확정
- **정체성**: NetEase Cloud Music A&R/라이선스 발굴·평가 도구.
- **데이터 소스**: 공개 스트리밍/소셜 + 외부 차트 벤더.
- **구조**: 코어(수집·분석·점수화) + 온디맨드 딜리버리 스킬.
- **평가 기준**: 4축(성장세·충성도·적합성·계약여지) + 맥락 종합 코멘트.
- **첫 결과물**: CLAUDE.md / CONTEXT.md만. 전체 폴더·코드 구조는 현 단계에 과하다고 판단 → 점진 확장.
- **문서 언어**: 한국어 (코드 주석·식별자는 영어).
- 출처: [옵시디언 "뮤직라이센스 프로젝트" 노트] + 착수 브레인스토밍.

### #002 (2026-06-18) — 첫 스킬 `artist-pickup` 빌드
- **정체성**: 아티스트 후보 풀을 받아 6기준으로 점수화·랭킹해 라이선스 shortlist를 근거와 함께 산출.
- **입력 모델**: 후보 풀 랭킹(여러 명 → 정렬). 단일 아티스트도 가능.
- **데이터 출처**: 하이브리드 — 음악/스트리밍/세일즈는 정확성 1순위(Spotify API + 차트/웹), 나머지(국적·중국활동·나이·인디/메이저)는 웹서칭/리서치 스킬. **API에 갇히지 않음.**
- **6기준 비중**: 스트리밍 21 · 장르(힙합/R&B/POP) 21 · 인디/메이저 21 · 팔로워 14 · 카탈로그 14 · 중국활동 7. 전부 소프트(하드 필터 없음). 나이는 점수 아닌 저장 항목.
- **정규화**: 노트 명시 기준은 절대 앵커(IG 50만↑, 장르, 인디>메이저), 연속 수치는 풀 내 상대.
- **출력**: 채팅 랭킹표+스코어카드 + `reports/artist-pickup-<라벨>.md` 저장.
- **형태**: `.claude/skills/artist-pickup/` — SKILL.md + `scripts/spotify.py`(stdlib fetch)·`scripts/score.py`(가중·랭킹·리포트 렌더).
- **나중**: 유료 차트 벤더(Chartmetric) 연동, 노션/엑셀 딜리버리 스킬.
- 출처: [옵시디언 "아티스트픽업" 노트] + 결정 포인트 4개 합의(입력모델·데이터출처·하드vs소프트·비중) + 출력형태.

### #003 (2026-06-18) — 타깃 논지 & 발굴 데이터 획득법 (첫 발굴 회고)
- **타깃 = 서양권 우선 (화이트스페이스 전략).** 중화권 아티스트는 이미 중국 시장을 섭렵 → 라이선스 가치가 낮음. 반대로 **서양권 힙합/R&B/POP 아티스트를 중국 시장에 들여오는 것**이 공백을 메우는 발굴이다. → 발굴 렌즈 기본값을 "서양권 라이징 인디"로.
- **함의**: ④ 계약 가능성/공백 + ③ 지역 적합성은 "이미 중국에서 큰가"가 아니라 "**중국에 아직 안 들어왔지만 들여올 만한가**"로 읽는다.
- **발굴 데이터 획득법 개선**(SKILL.md 반영): IG/YT 팔로워·기본정보는 맹목적 웹서칭 대신 **신뢰 정보처(나무위키·네이버·Wikipedia)에서 기본정보+공식 SNS 링크 추출 → 그 링크를 `insane-search`로 접속해 실제 수치 확인**. 출처·신뢰도 기록, 확인 못 하면 null.
- 출처: 첫 발굴 회고(중화권 편중 발견) + 사용자 피드백.

### #004 (2026-06-18) — 첫 딜리버리 출력: HTML 검수 도시에(dossier)
- **무엇**: scored JSON → 깔끔한 standalone HTML로 렌더하는 `scripts/render_html.py` 추가. 6개 기준·점수·근거·신뢰도를 한눈에 검수 가능. (사용자 요청: "직관적·깔끔한 HTML로 확인·검수")
- **디자인**: NetEase 크림슨 1색 강조 + 모노 숫자, 시그니처 = "기여도 스트립"(너비=비중·채도=점수)로 종합점수 출처를 시각화.
- **아키텍처**: 표현(HTML)을 코어 점수화(score.py)에서 분리 — "코어 안정·표현은 스킬" 원칙대로 별도 렌더 스크립트. 향후 노션/엑셀도 같은 패턴으로 추가.
- **재현 파이프라인**: `data/<label>.json`(발굴·채점 입력) → `score.py` → `render_html.py --out reports/<label>.html`.
- 산출물: `reports/artist-pickup-western-rising-2026.html` (서양권 5인).
- 출처: 사용자 요청(검수용 HTML) + frontend-design 가이드.

### #005 (2026-06-18) — 세션 종료 루틴 표준화 & 1차 핸드오프
- **표준 루틴**: 사용자가 "세션 종료"를 요청하면 매번 *결정 로그 갱신 + 「현재 상태 & 다음 세션 이어가기」 최신화 + 영속화 확인 + 교훈 저장 + (요청 시)패키징*을 수행한다. (→ CLAUDE.md "세션 종료 루틴")
- **핸드오프**: 오늘 폴더를 zip으로 만들어 타인에게 전달 → 그 사람이 직접 GitHub 레포에 푸시 예정. README.md·.gitignore 정비.
- 출처: 사용자 요청.

### #006 (2026-06-22) — 루브릭 개편: 6점수축 → 4점수축 + 2비점수 의견
- **변경**: 점수 기준을 4개로 축소 — ① 스트리밍(Spotify + 주요 글로벌 플랫폼) 30% ② 장르 30% ③ SNS 팔로워(IG 중심) 20% ④ 카탈로그 20%.
- **인디/메이저 · 중국 활동은 점수에서 제외.** 대신 각 아티스트마다 **한 줄 의견(`takes`)**으로 스코어카드에 "참고 의견(점수 미반영)" 표시. 라이선스 판단엔 중요하지만 한 줄 숫자로 환원하면 왜곡되는 정성 영역이라는 판단.
- **스트리밍 정의 확장**: Spotify 단독 → Apple Music·YouTube Music 등 다른 글로벌 플랫폼 수치가 있으면 함께 블렌드(한 플랫폼 편중 보정).
- **가중치 근거**: 기존 6축 상대 비율(높음 21% : 중간 14%)을 4축으로 재정규화 → 30/30/20/20.
- **반영 파일**: `score.py`(DEFAULT_WEIGHTS·CRITERIA_LABELS·TAKE_LABELS·렌더), `render_html.py`(CRITERIA 4개·TAKES 섹션·헤더 "4기준"), `SKILL.md`(루브릭·입력계약·비점수 영역). 스모크 테스트 통과(가중치 재정규화·takes 렌더 확인).
- 출처: 사용자 요청.

### #007 (2026-06-23) — 딜 제안서 워크플로 신설 & `deal-proposal` 스킬 빌드
- **맥락**: NetEase가 아티스트(가칭 "A")에게 보낼 **독점 솔로 EP 딜 제안서**(`proposal_reference.docx`)를 분석 → 정정 → 비전문가용 docx로 재조판 → 스킬화.
- **두 기준 문서 신설** (`docs/`): 
  - `deal-proposal-context.md` = 딜 *내용* 기준(확정 딜 구조·결정·전문 용어 정책·열린 질문).
  - `deal-proposal-design-system.md` = *디자인/언어* 기준(컬러·타이포 토큰·컴포넌트·보이스).
- **딜 내용 확정**(원본 대비 수정): ① 중국 독점 3년 + 2년 우선갱신권 ② EP 최소 4곡, **타이틀곡만** Amy Allen 공동 프로듀싱(원본 "4곡 전부"에서 축소 — 예산 현실성) ③ 마스터 **중국만** NetEase 소유, 글로벌은 레비뉴 셰어만 ④ MV 권리 **제외** ⑤ 브랜드/CF 딜 **제외** ⑥ 준거법=**중국 본토법+중국 중재** ⑦ 제작비 **$100,000 단일**(원본 10만/15만 충돌 해소) ⑧ 글로벌 셰어율 인하(마스터 미소유 반영: A 85%/B 75%, 싱크 80%/70% — 제안값) ⑨ Option A 리스크 문구 정정("아티스트 위험부담" → "advance never repaid").
- **금융 구조 모델링 교훈**: Option A(선급 $250k recoupable·높은 셰어) vs B(MG $200k non-recoupable·낮은 셰어)는 **둘 다 합리적** — 저조 시 A, 중박 시 B, 대박 시 A 우위. "어느 게 좋다"가 아니라 기대 시나리오로 안내.
- **디자인 방향 확정**: 출력=**docx 단독** / 톤=**프리미엄 라이트 에디토리얼**(세리프 Georgia 산문 + 산세리프 Arial 데이터 페어링) / 브랜딩=**중립 프리미엄**(NetEase 레드 강조 안 함, 모노크롬 잉크+버건디 액센트) / 언어=**쉽게 쓰되 MG·sync·perpetual 등 정확한 용어 그대로**(상세 설명은 대면).
- **`deal-proposal` 스킬 신설** (`.claude/skills/deal-proposal/`): 기존 스킬 참고 없이 독립 신설. SKILL.md는 **시작 전 위 두 docs를 필수로 읽고** 그 기준을 따르도록 강제. 조판은 **JSON 딜 스펙 → docx 렌더러**(`scripts/build_proposal.js`, 디자인 토큰 내장=시스템 구현체) + 발송 전 정합성 체크리스트 6개 내장. 예시 입력 `examples/sample-deal.json`.
- **산출물**: `reports/deal-proposal-v1-mockup.docx`(v1 시안, 플레이스홀더 `[ARTIST]`+글로벌 제안값). 렌더러로 동일 재현 검증.
- **CLAUDE.md 갱신**: 톤앤매너에 "답변은 항상 사람이 이해하기 쉽고·구체적·직관적으로"(결론 먼저·추상어보다 구체값·직관적 구조·선택지엔 추천) 추가.
- 출처: 사용자 요청(제안서 분석 → 다듬기 → 디자인 협의 → 스킬화) + 금융구조 모델링.

### #008 (2026-06-26) — Tzuyu 제안서 = 승인된 **기본 양식/벤치마크** 확정 & 딜 구조 갱신
- **벤치마크 승격**: Tzuyu 솔로 EP 제안서(0626판)를 **앞으로 모든 아티스트 피칭 자료의 기본 양식·벤치마크**로 확정. 기준 파일 `.claude/skills/deal-proposal/reference/deal-proposal-benchmark-tzuyu-2026-06-26.docx`, 재현 스펙 `data/tzuyu-input.json`, 클론 템플릿 `examples/sample-deal.json`(전부 이 구조로 갱신).
- **7섹션 골격 표준화**: 01 Production(후크 인물) → **02 Marketing & Promotion(신설)** → 03 Deal Overview → 04 Financial(A/B) → 05 Revenue Share → 06 Key Terms → 07 Next Steps.
- **딜 구조 갱신**(원본/#007 대비): ① 중국 독점 **3년→5년** + 2년 갱신권 ② **프로모션 섹션 신설**: NetEase OOH 빌보드 30개 도시(베이징·상하이·광저우 등), 빌보드당 ≈RMB 150k(≈USD 20k) ③ **레비뉴 셰어 전면 개편** — 운영비 선공제 후 분배 모델: 중국 정액 20%(실물 30%), 글로벌은 실 플랫폼/유통 수수료 공제; 분배는 **옵션 내 균일 — A 50/50, B 40/60** ④ **실물앨범 = 포토카드 필수 제작** 조항 신설.
- **정합성 정정**: 0626 원본의 §05 note가 "스트리밍/싱크는 양 옵션 동일"이라고 적혀 있으나 B가 40/60으로 바뀌어 모순 → 벤치마크 스펙에서 "옵션 내 균일(A 50/50, B 40/60)"로 수정.
- 출처: 사용자 요청("이 문서를 기억하고 벤치마킹·기본 양식으로, 푸시·커밋").

### #009 (2026-08-10) — 사내 도구 = 网易POPO 확인, 자동화 경로 확정
- **맥락**: 사용자 NetEase 입사. 사내 협업 앱이 **网易POPO**로 확인됨 → Claude 자동화를 붙일 수 있는지 조사.
- **결론**: **붙는다.** 접점은 사내망 `popo.163.com`(공인 DNS도 사설 IP `10.82.112.24` 반환 = 외부 불가)이 아니라 **공개 도달 가능한 `open.popo.netease.com` 개방플랫폼**. 봇 롱커넥션 게이트웨이 `nws.popo.netease.com:11012`도 외부에서 열려 있음 → **VPN 없이 자동화 가능**. 단 문서·콘솔·앱 등록은 **corp SSO + 심사** 필요.
- **자동화 경로 우선순위**: ① 그룹 **웹훅**(심사 없음, 발신 전용 — 오늘 당장 가능) → ② **애플리케이션 봇**(appKey/Secret → accessToken, 발신 `im/send-msg` + 수신 **STOMP 롱커넥션**, 이벤트 AES-128-CBC) → ③ **cloud-zone mtable/캘린더/결재 API**(artist-pickup 결과 사내 표 적재) → ④ 데스크톱 UI 자동화는 **비권장**(공식 API 있음 + 보안정책).
- **아키텍처 판단**: POPO는 **코어가 아니라 딜리버리 채널**이다. `render_html.py`·`build_proposal.js`와 같은 층 — POPO 발신을 **MCP 서버/딜리버리 스킬**로 감싸고 점수화 코어는 손대지 않는다. (원칙 #1)
- **기준 문서 신설**: [docs/popo-automation-context.md](./docs/popo-automation-context.md) — 도메인 지형·프로토콜·제약·SSO 로그인 후 확인할 문서 6건. 신뢰도를 `[공식]/[교차]/[추정]`으로 표기(원칙 #3).
- **주의**: GitHub에 실 POPO 웹훅 토큰 다수 노출 확인 → 우리 토큰은 `.env`만. 사내 데이터 외부 반출 금지.
- 출처: 공식 도메인 실측(DoH·TCP·무인증 API 프로브) + 개방플랫폼 문서 라우트 2,000여 개 추출 + 제3자 실사용 코드(dify 플러그인·openloom·n8n-nodes-popo) 교차검증.

### #010 (2026-08-10) — 봇 읽기 범위 확정 & **1순위 재정의: 중국어 소통**
- **봇 읽기 범위 확정**: 봇 이벤트는 **4종뿐이고 전부 "봇에게 온 것"** — 봇 1:1 메시지, 그룹 @멘션, 그 둘의 철회. **내 1:1 대화·그룹 일반 대화·구독 채널·합류 이전 히스토리는 안 온다.** 봇은 "나"가 아니라 **별도 인격**이며 내 계정 권한을 물려받지 않는다. → **"사내 대화를 긁어 분석"은 설계상 불가능한 방향**이고, POPO가 의도한 건 *내보내기 + 호출받기*다. 우리 아키텍처(코어=점수화, POPO=딜리버리)와 일치.
- **단, 전달(forward)은 통째로 받는다** — 이벤트에 `merge_list`(합병 전달)·`file_info`(파일)·`quote_info`(인용) 존재. **POPO 안에서 대화 뭉치·문서를 봇 1:1방으로 전달하면 봇이 다 받는다** = 번역/요약 워크플로의 근거. 방식은 조회가 아니라 **푸시**.
- **설계 원칙 신설 — "봇은 나에게만 말한다"**: 봇은 나와의 1:1방에서만 응답, 그룹엔 발송 없음. 답변 초안은 만들어만 주고 **발송은 사람이 손으로**. 오발송 위험 구조적 0 + 권한 신청 설명이 깔끔("개인 보조용, 그룹 발송 없음").
- **1순위 재정의**: 지금 실제 병목은 발굴·점수화가 아니라 **중국어 소통**. 사용자 병목은 ① 단어 의미 파악 ② **뜻은 알아도 발음·성조를 몰라 매번 따로 찾음** → *번역* 문제가 아니라 **해독+학습** 문제. 일반 번역기로 안 풀린다.
- **핵심 지렛대 = 한국 한자음 매핑**: 报告=보고, 确认=확인, 版权=판권… 모든 출력에 **한자음 열 필수**. 의미 암기 비용이 0이 되고 성조만 남는다. 일반 번역기가 못 하는 차별점. (함정도 표기: 爱人=배우자, 工作=일)
- **출력 5단 포맷 확정**: ① 결론 한 줄(액션·기한) ② 문장 분해(원문/병음/한국어) ③ 단어장(+한자음) ④ **뉘앙스**(我先看看=보류·辛苦了=화제종료·麻烦一下=지시·方便的话=기한통보) ⑤ 답변 초안(중국어+병음+역번역). → [docs/cn-assistant-spec.md](./docs/cn-assistant-spec.md)
- **환경 확정**: POPO=회사 지급 PC / Claude Code=개인 맥 / **회사 PC에 Claude 데스크톱 설치 가능** → 붙여넣기 루프가 **회사 PC 안에서 완결**된다(파일·텍스트 이동 불필요). 그래서 A(회사PC Project 지침)를 먼저 만들고 실사용 후 B(스킬)·C(용어집).
- **컴플라이언스 라인**: 사내 메시지를 claude.ai에 붙여넣는 건 **사외 반출**. 계약 조건·미공개 아티스트 정보·금액·개인정보는 **빼고** 붙인다. IT Helper에 외부 AI 사용 가부 확인 권장.
- **사용자 지시(유효)**: 사용성 검증 단계 동안 **발송·수정·삭제 등 외부로 나가는 동작 전면 보류.** 명시적으로 풀어주기 전까지 조사·설계만.
- 출처: dify 플러그인 이벤트 구조 코드 정독 + 개방플랫폼 라우트 재분석 + 사용자 POPO 화면 확인(VPN 미연결 상태에서 정상 동작 = VPN 불필요 재확인) + 사용자 병목 진술.

### #011 (2026-08-10) — `grilling` 협의 규약 도입 & 레포 정본 확정
- **`grilling` 스킬 도입** (`.claude/skills/grilling/`) — dolphin-system 원본을 **sha256 동일**하게 복사(출처 `mattpocock/skills`). CLAUDE.md에 「설계·계획 협의」 절 신설: ① **한 번에 한 질문** ② 질문마다 **내 추천 답** ③ **사실은 내가 찾고 결정만 사용자에게** ④ **합의 전 실행 금지**. **사용자가 안 불러도 설계 낌새("설계"·"계획"·"새 스킬"·"만들자"·"어떻게 할까"…)가 있으면 내가 먼저 켠다.** 목적은 질문이 아니라 **사용자가 못 채운 맥락을 끌어내 축적하는 것** → 얻은 답은 그 턴에 바로 레포로.
  - `grill-me`는 dolphin-system에서 2026-07-24 삭제된 껍데기 별칭 — **입구는 `grilling` 하나**.
- **레포 정본 확정**: [`github.com/jerzjerz/musiclicense`](https://github.com/jerzjerz/musiclicense)를 앞으로도 이 프로젝트의 **정본 원격**으로 쓴다(사용자 결정). 다른 사람이 clone 후 바로 이어받을 수 있게 세팅하는 것이 요건.
- **⚠️ 확인된 사실 — 이 레포는 PUBLIC이고 소유자가 타인이다.** 익명(비로그인) API·웹 모두 200 응답으로 확정. 사용자 계정 `wonmotime-1`은 collaborator라 **push는 되지만 `admin: false` → 공개 여부를 바꿀 수 없다.** 소유자 `jerzjerz`만 가능. 즉 커밋하는 모든 내용은 공개된다(현재 Tzuyu 딜 조건 포함). **사용자가 이 상태를 인지하고 진행하기로 결정.** fork 0.
- **README 전면 현행화**: 폐기된 **6축 루브릭이 그대로 적혀 있던 문제**를 4점수축+2의견으로 정정. 스킬 4개 표·읽는 순서·셋업 절차(`deal-proposal`은 `npm install --prefix` 1회 필요) 추가 — clone 후 바로 쓸 수 있게. `.gitignore`에 `node_modules/` 추가.
- **봇 권한 1차 확인**: 사용자가 회사 PC POPO **그룹 설정에서 `Robot` 메뉴 존재를 확인**. → 웹훅(L0) 경로가 열려 있을 가능성이 높아졌다. 실제 URL 발급·발송 테스트는 **외부 동작 보류 지시에 따라 미실행**.
- **새 스킬 아이디어 2건 접수(미설계)**: ① 단톡방 캡쳐 → 맥락·흐름 분석 (→ `cn-assistant-spec.md`의 A/B와 사실상 같은 줄기) ② **지출 증빙 정리** — 영수증 사진·자료를 건별로 분리해 입력 방법을 안내. 데스크톱 등록 절차는 사용자가 다음 세션에 컨텍스트 제공 예정. **둘 다 grilling 미완 — Q2(배치·범위)부터 재개.**
- 출처: 사용자 요청(grilling 셋팅·레포 정리·푸시) + 레포 가시성 실측(익명 API/웹 200, `admin:false`) + 사용자 판단.

### #012 (2026-08-10) — 커밋·푸시 규약 확정: **자동 푸시 + 작은 단위 커밋**
- **푸시 자동화**: 사용자가 요청하지 않아도 **작업 마무리·중요 기점마다 Claude가 알아서 푸시한다.** 시점 = ① 작업 단위 완료 ② 문서·스킬 신설/개편 ③ 결정 로그 추가 ④ 세션 종료 ⑤ 방향 전환 직후. **이유**: 로컬에만 쌓이면 다른 컴퓨터·다른 사람이 이어받지 못한다 — 이 프로젝트는 애초에 "clone 후 바로 이어받기"가 요건(#011).
- **예외 하나**: 이 레포는 PUBLIC + 타인 소유(#011)이므로, 새로 커밋되는 내용에 **공개되면 곤란한 것**(사내 기밀·계약 금액·개인정보·토큰)이 섞이면 **푸시 전에 한 줄 알리고 판단을 받는다.** 막는 게 아니라 알리는 것.
- **커밋은 한 커밋 = 한 목적, 작게·자주.** 목적: **커밋 목록만 보고 작업 단위를 파악할 수 있게.** 분리 기준 = 문서 신설 / 스킬 추가 / 규약 변경 / 기능 / 버그 수정 / 리팩터 / 리포트 산출. 작업 도중에도 의미 단위가 끝나면 즉시 커밋(세션 끝에 몰아치기 금지).
- **반례를 규약에 박았다**: 같은 날 `7bc2b85`가 문서 2종 + 스킬 도입 + 규약 변경 + README 개편 + `.gitignore`를 **8파일 한 커밋**에 몰아넣은 것. 최소 4개로 쪼갰어야 했다. → [CLAUDE.md 「커밋·푸시 규약」](./CLAUDE.md)
- 출처: 사용자 지시("푸시 요청 안 해도 자동화, 커밋은 작업 단위를 이해할 수 있게 최대한 작은 단위로 자주").

### #013 (2026-08-10) — 중국어 보조 **A 지침 완성** & 입력 범위 = 텍스트 전용
- **입력 범위 확정 = 중국어 텍스트 붙여넣기만.** 스크린샷·파일은 범위 밖(사용자 선택 ①). **근거**: ① 컴플라이언스 라인이 명확해짐("텍스트만, 민감정보는 빼고") ② 캡쳐엔 무관한 대화·동료 이름·워터마크가 딸려 들어가 **사외 반출 범위가 넓어짐** ③ 신입이 사내 화면을 외부 AI에 올리는 습관이 붙는 게 위험 ④ 지금 병목(단어·성조)엔 텍스트로 충분(YAGNI). 필요하면 사용자가 **개별 판단해서** 캡쳐를 던진다.
- **A 완성** → [docs/cn-assistant-project-instructions.md](./docs/cn-assistant-project-instructions.md). **자립적으로 작성** — 회사 PC의 Claude는 이 레포에 접근할 수 없으므로 5단 포맷·한자음 원칙·완곡어법 사전·함정 목록을 **전부 본문에 박았다.**
- **spec에 없던 운영 규칙 6개 추가** (실사용성 보강, 다음 세션에 spec으로 흡수 검토): ① **짧은 메시지엔 짧게** — `收到` 같은 한 줄엔 ①③만(분량은 원문에 비례) ② **사람 이름·부서명**은 한자+한글+병음 함께 ③ **날짜·기한을 실제 의미로** 풀기(`下周之前`→"다음 주 시작 전") ④ **모르면 `(추정)` 표시**, 지어내지 않기 ⑤ 텍스트만 던지면 = "5단으로 정리해줘", 되묻지 않기 ⑥ 민감정보가 명백히 섞이면 **한 줄 짚고 그대로 진행**(막지 않음).
- **완곡어법 사전 10개·한자음 함정 14개**로 확장(spec의 5개·4개 대비). `尽快`=오늘~내일 압박, `应该没问题`=확답 아님, `检讨`=반성(검토 아님), `合同`=계약서 등.
- **다음**: 며칠 실사용 → 불편한 지점 수집 → **B**(`popo-cn` 스킬)·**C**(`data/cn-glossary.md` 용어집). 완성도보다 실사용 피드백이 먼저.
- 출처: grilling 질문(입력 범위 ①/②/③)에 사용자가 ① 선택 + `cn-assistant-spec.md` 5단 포맷 구현.

### #014 (2026-08-18) — 출장비 정산 엑셀 자동 기입 스킬 설계 착수 (**grilling 미완**) & 중국어 전달 규약
- **백로그 6-2 「지출 증빙 정리 스킬」이 구체화됐다.** 미파악이던 시스템 정체가 확인됨 = **NetEase OA `差旅报销`(출장비 정산)**, 업로드 파이프라인은 사내 워크플로 엔진 **easeflow(易流)**. 사용자가 템플릿 4종을 직접 받아 제공.
- **템플릿 4종 구조 실측 완료** → [docs/expense-templates.md](./docs/expense-templates.md). 핵심: **전 항목 필수(`*`)**, 데이터는 **2행부터**, 숨김 시트 `_easeflow_import_info_`에 **파일별 식별 토큰**이 있어 **새 워크북을 만들면 안 된다**(복사해서 행만 추가). 서식이 하나도 정의돼 있지 않아(`numFmts count=0`) **우리가 넣는 타입이 곧 규약**이 된다.
- **함정 2개를 문서에 박았다**: ① **기차표는 `交通费`(교통비)가 아니라 `机票、火车票`(항공권·기차표) 파일** — 그 파일 G열 `交通工具`(교통수단) 드롭다운에 `火车`·`高铁*`가 실제로 들어 있다. 잘못 넣으면 임포트는 통과해도 엉뚱한 항목으로 정산된다. ② `交通费` 템플릿에만 **`是否国际/港澳台出差`(국제출장 여부) 열이 빠져 있다** — 화면에선 필수인데.
- **확정 5건** → [docs/expense-context.md §3](./docs/expense-context.md): ① 레포엔 **스킬 로직·문서만** 커밋(실데이터·토큰·실명은 `.gitignore`) ② 템플릿은 **재사용**(마스터 봉인 + 폴더마다 복사) ③ **폴더 1개 = 출장 1건**(제출 단위와 일치, 반복값이 폴더 내 고정) ④ `币种`(통화) 표기는 `CNY-人民币` 형식(코드만이 아님) ⑤ **산출물은 엑셀뿐** — 첨부·리네임·브라우저 자동화는 범위 밖.
- **사용자 교정 2건 (교훈)**:
  - **"영수증을 한 장도 안 보고 판독 한계를 단정하지 마라."** 처음에 "이 칸들은 영수증에 절대 안 나온다"고 썼다가 지적받았다. → 값의 출처를 **①직독 / ②유도 / ③영수증 밖** 3등급으로 갈라서 말하는 방식으로 교체. 실제로 ③으로 남는 건 **동석자·동숙자 이름**과 **`详细说明` 문구 규칙** 둘뿐이었다.
  - **"화면 폼 말고 엑셀 기입에 집중하라."** 첨부 파일 정리·업로드 보조를 제안한 건 스코프 확대였다. v1은 **엑셀 셀을 정확히 채우는 것** 하나.
- **중국어 전달 규약 신설** → [CLAUDE.md 「중국어를 다루는 법」](./CLAUDE.md). 뜻만 알려주면 사용자가 **화면에서 그 글자를 못 찾는다.** 그래서 **원문+한국어 병기**, **파일→열→항목명으로 위치 특정**, **드롭다운 선택지는 원문 그대로 전부 나열**, 중국어 3개 이상이면 표. 출처: 사용자 지시.
- **미완**: `grilling` 질문 6까지 마치고 **질문 7(영수증 → 파일 분류표 확정)에서 중단.** 미확정 14건. **영수증 실물을 아직 못 봤다** — 사용자가 샘플 제공 예정. 코드·스킬은 아직 만들지 않았다(합의 전 실행 금지).

---

### #015 (2026-08-24) — **출장비 정산 첫 완주**: 옌타이 출장 4개 파일 산출 + 서버 오류 로그로 규약 3건 실증

**무엇을 했나**: 카카오톡으로 받은 영수증 묶음 PDF(10장)를 페이지별 PDF로 쪼개고, 위챗 전자증빙·항공·호텔 영수증까지 읽어 **`机票、火车票`·`酒店费用`·`个人餐费`·`交通费` 4종 엑셀을 전부 채웠다.** TPE 때는 3종·부분이었고 호텔은 템플릿이 없어 못 만들었다.

**규약을 「추정」에서 「실증」으로 끌어올린 3건** → 상세는 [docs/expense-context.md](./docs/expense-context.md) D14~D16
1. **`就餐人`은 사원 마스터 조회 칸** (D15). 지난 업로드의 **서버 오류 로그 파일을 찾아 뜯어본 결과** — 반려 사유가 `字段[就餐人]的人员[1]不存在` 딱 하나였다. **결정 D10(이름 칸 비우기)이 실증됐고**, 동시에 열린 질문 **3(통화 형식)·8(값 타입)이 종결**됐다. 나머지 칸은 전부 통과했기 때문이다.
2. **템플릿 토큰은 다운로드마다 새로 발급된다** (D14). 8/18본과 8/24본의 토큰이 달랐다. **D2의 「마스터 봉인 후 계속 복사」는 폐기** — 출장마다 새로 받는다.
3. **중국 본토 출장의 `国际出差地区` = `中国`** (D16, 열린 질문 9 종결).

**교훈 — 서버 오류 로그는 그 자체로 스펙 문서다.** 임포트가 튕기면 로그 파일을 반드시 받아서 뜯어본다. 우리가 「미검증」으로 6일간 들고 있던 가정 3개가 그 파일 한 장에 답이 있었다.

**교훈 — 못 읽는 PDF는 렌더링해서 눈으로 읽는다.** Trip.com 호텔·위챗 전자증빙은 `/ToUnicode`가 **항등 CMap**이라 `pdftext.ps1`로도 못 뚫는다(매핑 정보 자체가 없음). Windows 내장 PDF 래스터라이저(WinRT `Windows.Data.Pdf`)로 PNG를 뽑아 읽었다. → `tools/pdfrender.ps1`. **이제 이 PC에서 「못 읽는 PDF」는 사실상 없다.**

**도구 4종 신설** (`tools/`): `pdfrender.ps1`(PDF→PNG), `pdfsplit.ps1`(묶음 PDF→페이지별, 무손실), `xlsx-addrows.ps1`(백지 템플릿에 행 주입 — 숨김 토큰 시트 보존), `xlsx-clearrows.ps1`(채워진 파일을 백지로 되돌리기).

**아직 스킬로 만들지 않았다.** 이번엔 손으로 완주하며 규약을 실증하는 게 먼저였다. 업로드가 실제로 통과하면 그때 스킬화한다(YAGNI).

### #016 (2026-08-31) — **`expenses` 폴더 레포 편입 + 국제출장 판정 규칙 전면 수정**: 못 본 오류 로그 3장이 결정 2건을 뒤집었다

**무엇을 했나**: 사용자의 출장비 작업 폴더를 레포 안으로 들이고(`music/expenses/`), 영수증 자동 분류 워크플로를 `grilling`으로 합의했다. 그 과정에서 **`Downloads`에 방치돼 있던 8/24 서버 오류 로그 3장을 발견**해 뜯어봤고, 확정돼 있던 결정 2건이 틀렸다는 게 드러났다.

**뒤집힌 결정 2건** → 상세는 [docs/expense-context.md](./docs/expense-context.md) D17
1. **~~D12~~ 폐기 → D17.** 국제출장 판정은 「출장 단위 고정값」이 아니라 **「비용이 발생한 나라」 단위**다. 한국에서 쓴 돈은 `是`+`韩国`, 중국 본토에서 쓴 돈은 `否`+지역 비움. **한 정산서 안에서 행마다 값이 갈린다.** 사용자 설명으로 풀렸다 — 회사는 중국 법인, 사용자는 한국 근무라 회계 기준국이 중국이다.
2. **~~D16~~ 폐기.** `国际出差地区`(국제 출장 지역)는 자유 입력이 아니라 **해외 지역 마스터 목록 조회 칸**이었다. `中国`은 애초에 목록에 없다(중국 회사에 중국은 해외가 아니다). 서버: `字段[国际出差地区]的值[中国]在数据源中不存在`.

**새로 알아낸 것 2건**
3. **⚠️ Excel로 열어서 저장하면 임포트 토큰이 삭제된다** (D19). 숨김 시트가 `<sheetData/>`만 남는다. 타이베이 산출물 3개가 이미 그 상태였다 — **레포 사본이 온전한 정본이었고 작업 폴더 쪽이 손상본이었다.** 복구해서 합쳤다.
4. **D14는 근거가 약하다.** 토큰 전수 비교 결과 표본이 `个人餐费` 한 쌍뿐이고, 그 둘은 **서로 다른 정산서**에서 받은 것이다. 「다운로드마다 랜덤」인지 「정산서마다 고유」인지 못 가린다 → 봉인본 재사용으로 운영하며 첫 업로드로 판별.

**구조 결정 3건**: D18(파일명 `{도시}_{상호}_{화폐-금액}_{날짜}[_{증빙종류}]`), D20(`expenses/` 레포 편입 + 통째 gitignore), D21(카테고리 5종 + `flight/train`).

**교훈 — 「확정」에 날짜를 박아둬도 근거를 다시 세어봐야 한다.** D12·D16은 확정 표시가 붙은 채 6일을 살아 있었지만 실증 근거가 없었다. D14도 표본이 1개뿐인데 단정문으로 적혀 있었다. **확정문에는 표본 수를 같이 적는다.**

**교훈 — 산출물을 열어보는 행위 자체가 산출물을 망가뜨릴 수 있다.** 검수는 엑셀이 아니라 `검수표.md`로 한다(D19).

**교훈 — 사용자가 "private이다"라고 해도 확인한다.** 영수증을 레포 안으로 들이기 전 익명 API·`raw.githubusercontent`·`git ls-remote` 3중으로 확인했고 **PUBLIC이었다.** 그대로 진행했으면 카드 결제내역이 공개됐다.

---

## 현재 상태 & 다음 세션 이어가기
> **다음 세션은 이 섹션부터 읽으면 바로 이어갈 수 있다.** (최종 갱신: **2026-08-31**)

### 🚦 지금 어디에 있나 (2026-08-18 기준)
사용자가 **2026-08 NetEase 입사** 후, 무게중심이 딜/발굴에서 **사내 업무 자동화**로 옮겨가 있다. 코드는 아직 한 줄도 안 짰다 — **전부 조사·설계 단계.**

**⛔ 유효한 사용자 지시**: 사용성 검증 단계 동안 **발송·수정·삭제 등 외부로 나가는 동작 전면 보류.** 명시적으로 풀어주기 전까지 조사·설계만 한다.

**🔴 지금 진행 중 — 출장비 정산 엑셀 자동 기입 스킬 (결정 #014)**
`grilling` 협의를 **질문 6까지 마치고 질문 7에서 멈춰 있다.** 다음 세션은 여기부터 이어간다.
- 읽을 것: [docs/expense-context.md](./docs/expense-context.md) (결정·열린 질문·재개점) → [docs/expense-templates.md](./docs/expense-templates.md) (셀 구조)
- **재개 지점 = 질문 7**: 영수증 → 4개 파일 **분류표 확정** + "개인 물품 구매"는 4종에 자리가 없는데 어떻게 할지
- **사용자가 줄 것**: **영수증 샘플 이미지.** 아직 실물을 한 장도 못 봤다 — 이게 있어야 값 출처 3등급(①직독 ②유도 ③영수증 밖)이 추정에서 실측으로 바뀐다.
- **⚠️ 새 컴퓨터에서 이어받을 때**: 템플릿 원본 `.xlsx` 4개는 **커밋되지 않는다**(결정 D1). 사내 시스템에서 다시 받아야 한다.

**이월 — 중국어 보조 A 지침 실사용**: [docs/cn-assistant-project-instructions.md](./docs/cn-assistant-project-instructions.md)의 **구분선 아래 전체를 복사** → 회사 PC Claude 데스크톱에 **새 Project 생성** → **프로젝트 지침**에 붙여넣기 → 며칠 실사용. 그 뒤 **B**(`popo-cn` 스킬)·**C**(`data/cn-glossary.md` 용어집), A의 운영 규칙 6개(#013)를 spec 정본으로 흡수할지 판단.

### 📋 다음 세션 시작 프롬프트 (그대로 복사해서 붙여넣기)
```
music-license 레포에서 이어서 작업하자.

먼저 이 순서로 읽어줘:
1. CLAUDE.md (특히 「중국어를 다루는 법」·「설계·계획 협의 = grilling」)
2. CONTEXT.md 맨 아래 「현재 상태 & 다음 세션 이어가기」
3. docs/expense-context.md  ← 결정·열린 질문·재개 지점
4. docs/expense-templates.md ← 템플릿 4종 셀 구조

출장비 정산 엑셀 자동 기입 스킬 설계를 grilling 질문 7(영수증 → 4개 파일
분류표 확정 + 개인 물품 구매 처리)부터 이어서 진행하자. 한 번에 한 질문씩,
추천 답을 붙여서.

영수증 샘플 이미지를 줄 테니 그걸로 값 출처 3등급(①직독 ②유도 ③영수증 밖)을
추정이 아니라 실측으로 확정하자. 템플릿 원본 xlsx 4개는 커밋되지 않으니
이 컴퓨터에 없으면 내가 사내 시스템에서 다시 받아올게.
```

### ✅ 지금까지 완성된 것
- **출장비 정산 기준 문서 2종** (`docs/`) — **이번 세션 신설** (결정 #014):
  - [`expense-templates.md`](./docs/expense-templates.md) — 템플릿 4종 **셀 구조 실측본**. 컬럼 원문+한국어 병기, 드롭다운 전체 값, `币种`(통화) 목록, 화면 폼과 엑셀의 차이, 재현 스크립트.
  - [`expense-context.md`](./docs/expense-context.md) — **도메인 지식 + 협의 기록**. 확정 5건 / 값 출처 3등급 / 잠정 분류표 / **열린 질문 14건과 재개 지점**.
- **POPO 기준 문서 2종** (`docs/`) — **이번 세션 신설**:
  - [`popo-automation-context.md`](./docs/popo-automation-context.md) — 도메인 지형·봇 프로토콜·**읽기 범위 확정**·설계 원칙("봇은 나에게만 말한다")·제약. 신뢰도 `[공식]/[교차]/[추정]` 표기.
  - [`cn-assistant-spec.md`](./docs/cn-assistant-spec.md) — 중국어 보조 **출력 5단 포맷 정본** + 워크드 예시 + A/B/C 구현 계획 + 컴플라이언스 라인.
- **방향 문서**: [CLAUDE.md](./CLAUDE.md)(HOW·톤앤매너+"쉽고·구체·직관" 원칙·**grilling 협의 규약**·세션 종료 루틴), [CONTEXT.md](./CONTEXT.md)(WHAT/WHY·결정 로그 #001~#011).
- **`grilling` 스킬** (`.claude/skills/grilling/`) — **이번 세션 도입**. 설계·계획 논의 시 한 번에 한 질문씩 결정 트리를 내려가 맥락을 끌어낸다. Claude가 안 불러도 스스로 켠다. (결정 #011)
- **[README.md](./README.md) 현행화** — **이번 세션**. 낡은 6축 루브릭 정정 + 스킬 4개 + 읽는 순서 + clone 후 셋업 절차. 새로 오는 사람은 이것부터 읽으면 된다.
- **`artist-pickup` 스킬** (`.claude/skills/artist-pickup/`): SKILL.md(4기준 루브릭+비점수 의견 2)·`spotify.py`·`score.py`(코어)·`render_html.py`(HTML 검수 도시에). 발굴 데이터·리포트 영속(`data/`·`reports/`).
- **`deal-proposal` 스킬** (`.claude/skills/deal-proposal/`) — **이번 세션 신설**:
  - `SKILL.md` — 시작 전 `docs/`의 두 기준 문서 필수 독해 + JSON→docx 워크플로 + 발송 전 정합성 체크리스트 6개.
  - `scripts/build_proposal.js` — 딜 스펙(JSON) → docx 렌더러. 디자인 토큰 내장(=디자인 시스템 구현체). `npm install --prefix`로 docx 의존성.
  - `examples/sample-deal.json` — 현재 딜(= v1 시안) 예시 입력.
- **딜 제안서 기준 문서** (`docs/`): `deal-proposal-context.md`(딜 내용·결정·열린 질문), `deal-proposal-design-system.md`(디자인·언어 토큰).
- **산출물**: `reports/deal-proposal-v1-mockup.docx`(v1 시안). 원본 `proposal_reference.docx`.

### 🔧 다음 세션 TODO

**🔴 우선순위 0 — 출장비 정산 (규약 실증 거의 끝, 업로드 1회가 남은 관문)**
0-1. ✅ **워크플로·구조 확정** (결정 #016 → D17~D21). `expenses/`가 **레포 안(`music/expenses/`)** 으로 들어왔고 통째로 gitignore된다. 카테고리 5종 + `flight/train`, 파일명 `{도시}_{상호}_{화폐-금액}_{날짜}[_{증빙종류}]`. 봉인 템플릿 4종 = `expenses/_templates/*_BLANK.xlsx`.
0-2. ✅ **옌타이 `个人餐费` 재작성 완료** (2026-08-31). 8/24 업로드 3회 반려 사유(전 행 `国际出差地区`=`烟台市`)를 D17로 고쳤다 — 인천공항 2행 `是`+`韩国`, 옌타이 현지 4행 `否`+지역 셀 없음. 봉인 템플릿에서 새로 생성해 토큰도 온전하다. **업로드 대기.**
0-3. 🟡 **`国际出差地区` 목록 10개 확보, 전체는 아직** — `韩国`(1번)·`挪威`·`瑞士`·`冰岛`·`迪拜`·`澳门`·`台湾`·`南非`·`利比亚`·`安哥拉`. **`香港`(홍콩)은 미확인** — `HKF-202608` 정산할 때 `香`을 검색해 확인한다. 목록에 `中国`은 없다.
0-4. 🔵 **(사용자) 업로드 → 결과 회신.** 튕기면 **오류 로그 파일을 꼭 받아주세요** (D15의 교훈: 로그가 곧 스펙). 남은 미검증 5건이 이걸로 대부분 판별된다 → [expense-context.md §6](./docs/expense-context.md).
0-4b. ✅ **「회사 예약 항공권이 중국 국내선인데 `是`」 모순 해소** (D17-1). 그건 **출장 허가 신청서 층위**의 값이다 — 출장 전체가 한국 출발이라 `是`이고, 그 아래 구간이 상속받은 것. 우리가 채우는 **정산 행 층위**와는 다른 칸이니 그 값을 보고 엑셀을 정하지 않는다.
0-2b. ✅ **옌타이 `机票、火车票`도 D17로 수정 완료** (2026-08-31). `是否国际`가 `否`였는데 서울→옌타이는 한국 출발·결제라 `是`가 맞다. 봉인본에서 재생성. **4종 전부 D17 적용 + 토큰 온전 확인 → 업로드 준비 끝.**
0-2c. ✅ **`Daily 202608` 택시 2건 = 출장 무관 확정** (사용자, 2026-08-31). 귀국일 당일이라도 자동으로 출장에 넣지 않는다.
0-5. ⚠️ **`TPE-202608` 산출물도 D17 기준으로는 틀렸다** — 9행 전부 `Taiwan`이다. 김포공항 건만 `韩国`이어야 한다. 재업로드할 일이 생기면 수정.
0-6. **남은 미확정 3건** (금액·상호 등 상세는 gitignore되는 `expenses/YANTAI-202608/검수표.md`에 있다): ① 식비 1건이 증빙 파일 삭제로 빠져 있음(복원할지) ② `交通费` 행 5의 승하차 지점이 증빙에 없음 ③ 항공권 총액에 포함된 **여행자보험료**를 항공권으로 넣어도 되는지.
0-7. **업로드가 통과하면 그때 스킬 빌드.** 지금은 손으로 완주하며 규약을 실증하는 단계다(YAGNI).

**우선순위 1 — 중국어 소통 (오늘 당장 효과)**
1. ✅ **A 작성 완료** (2026-08-10, 결정 #013) → [`docs/cn-assistant-project-instructions.md`](./docs/cn-assistant-project-instructions.md). 입력 범위 = **텍스트 전용**. **사용자가 회사 PC Project에 붙여넣고 실사용할 차례.**
2. **(사용자) Step 0 확인**: POPO 사이드바 `AI` 버튼 / 메시지 우클릭 `翻译` 유무 → 이미 되면 우리 범위 축소.
3. 며칠 실사용 후 **B**(`popo-cn` 스킬) · **C**(`data/cn-glossary.md` 용어집). 이때 A의 운영 규칙 6개를 spec 정본으로 흡수할지도 함께 판단.

**우선순위 2 — POPO 권한 (1차 통과)**
4. ✅ **그룹 설정에 `Robot` 메뉴 존재 확인됨** (2026-08-10, 사용자). → 웹훅(L0) 경로가 열려 있을 공산이 크다. **단 실제 URL 발급·발송 테스트는 「외부 동작 보류」 지시로 미실행** — 지시가 풀리면 개인 테스트 그룹에서 먼저.
5. 권한 나오면 → **번역 봇 1:1**(전달→해석 회신). "봇은 나에게만 말한다" 원칙 준수.
6. 그 다음 → artist-pickup 리포트 POPO push, cloud-zone 다차원표 적재.

**우선순위 2.5 — 새 스킬 2건 (grilling 미완, Q2부터 재개)**
6-1. **단톡방 캡쳐 분석 스킬** — 캡쳐/텍스트를 주면 내용·맥락·흐름을 정리. 사실상 `cn-assistant-spec.md`의 A/B와 같은 줄기라 **중복 설계 주의**: 별도 스킬로 뗄지, `popo-cn` 하나로 합칠지가 미결.
6-2. ~~지출 증빙 정리 스킬~~ → **우선순위 0으로 승격, 진행 중** (결정 #014). 시스템 정체 확인 완료 = **NetEase OA `差旅报销` + easeflow 임포트**. 템플릿 4종 실측 끝. 위 「우선순위 0」 참조.

**우선순위 3 — 본업 (이월)**
7. **딜 제안서 실데이터 1부**: `docs/deal-proposal-context.md` §5 열린 값 채우기 → ① 글로벌 셰어 제안값(A85/B75·싱크80/70) 최종 컨펌 ② 아티스트 **실명** ③ 세금/원천징수 한 줄 넣을지 ④ Amy Allen 타이틀곡 실견적·가용성 사내 확인. 채워지면 `examples/`를 복제·수정해 실전 docx 렌더.
8. **스킬 트리거 테스트**: "딜 제안서 만들어줘"류로 `deal-proposal`이 의도대로 발동하는지 확인.
9. **(이월) artist-pickup 개선 3종**: HTML 포맷·`data/<label>.json` 스키마·검수 레이아웃 — 구체 지시 받기 전 먼저 무엇을 바꿀지 물어볼 것.

### 🗂️ 이월된 백로그
- LibreOffice 미설치 → docx 전체 페이지 PDF 프루프 자동 생성 불가(현재 QuickLook 표지 썸네일만). 필요 시 설치.
- Spotify API 키 세팅(artist-pickup 스트리밍 신뢰도 보강).
- 노션/엑셀 딜리버리 스킬(같은 분리 패턴).

### ❓ 열린 사실/주의
- **⚠️ 이 레포는 PUBLIC이고 소유자가 타인(`jerzjerz`)이다.** *(2026-08-31 재확인 — 익명 GitHub API `"private": false` / `raw.githubusercontent` HTTP 200 / 자격증명 없이 `git ls-remote` 성공. 3중 확인.)* 사용자 계정 `wonmotime-1`은 collaborator — **push는 되지만 admin이 없어 공개 여부를 바꿀 수 없다.** 즉 **커밋하는 모든 내용이 즉시 공개된다.** 사용자가 인지하고 진행하기로 결정(#011). 새 파일을 커밋할 땐 **공개돼도 되는 내용인지 한 번 더 확인**하고, 애매하면 사용자에게 알린다. 토큰·키는 당연히 `.env`만.
- **⚠️ 완성된 정산 엑셀을 Excel로 열어서 저장하면 임포트 토큰이 삭제된다** *(2026-08-31 실측, D19)*. 검수는 엑셀이 아니라 `검수표.md`로 한다. 실수로 저장했으면 백지 템플릿에 행을 다시 주입한다.
- **`币种`(통화) 형식 확정**: `CNY-人民币` 처럼 **전체 문자열**로 넣는다 — 업로드 오류 로그에서 통과 확인(D15).
- **템플릿 토큰 재사용 여부는 여전히 미판별** *(D14 근거 약화, 2026-08-31)*. 비교 표본이 `个人餐费` 한 쌍뿐이고 그 둘은 **서로 다른 정산서**에서 받은 것이다. 「다운로드마다 랜덤」인지 「정산서마다 고유」인지 못 가린다. **현재는 봉인본(`expenses/_templates/`) 재사용 전제로 운영** — 첫 업로드로 판별.
- **출장비 정산 폴더 `expenses/`는 레포 안에 있지만 통째로 gitignore된다** (결정 #016 / D20). 영수증·완성 엑셀·봉인 템플릿·`profile.json`(실명 포함) 전부 커밋 금지. 새 컴퓨터에서 이어받으면 **사내 시스템에서 다시 받아야 한다.** 숨김 시트 토큰 값·동료 실명도 문서에 적지 않는다.
- **바탕화면 `Desktop\expenses`는 완전히 제거됐다** (2026-08-31). 출장비 작업 폴더는 이제 **`music/expenses/` 한 곳뿐**이다 (75개 파일, 전부 gitignore 차단 실측 확인).
- **⚠️ `data/expense/`에 옛 사본이 남아 있다** (2026-08-31 — 삭제가 권한 정책에 막혀 보류). `expenses/`로 전량 이관·해시 검증은 끝났으니 **수동으로 지워도 안전**하다. 역시 gitignore 대상이라 방치해도 공개 위험은 없다.
- **POPO 권한 = 부분 확인.** 그룹 `Robot` 메뉴 존재는 확인됐다(#011) → 웹훅(L0)은 열려 있을 공산이 크다. 하지만 **개방플랫폼 콘솔에서 앱 봇을 만들 권한이 있는지는 여전히 모른다.** 앱 봇(L1~L3) 계획은 전부 조건부.
- **POPO 공식 문서는 한 페이지도 못 읽었다** (corp SSO 뒤). 프로토콜 세부는 전부 제3자 코드 기반(`[교차]`) — 실제 구현 전 공식 문서로 재확인 필수. **Claude가 사내 계정으로 대신 로그인하지 않는다.**
- **환경 분리**: POPO=회사 지급 PC / 이 레포·Claude Code=개인 맥. 회사 PC엔 Claude 데스크톱 설치 가능(확인됨). 개인 맥엔 POPO 미설치.
- **POPO는 VPN 없이 동작**(사용자 화면에서 "VPN Not Connected" 상태로 정상 사용 확인). 자동화도 VPN 불필요.
- `deal-proposal` v1 시안은 **플레이스홀더 상태**: 아티스트=`[ARTIST]`, 글로벌 셰어=context §4-1 *제안값*(미확정). 실발송 전 반드시 확정.
- 디자인 토큰을 바꿔야 하면 **`deal-proposal-design-system.md`를 먼저 고치고** 그 다음 `build_proposal.js`의 토큰 상수를 맞춘다(임의 손조판 금지).
- Spotify 키 미설정 → artist-pickup 자동 fetch 미작동, 웹서칭 대체 중.
