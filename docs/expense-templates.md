# 비용 정산 엑셀 템플릿 구조 (실측본)

> **무엇**: NetEase OA에 업로드하는 **엑셀 템플릿의 셀 구조 실측 기록.** 두 계열을 다룬다 —
> - **`差旅报销`(차뤼바오샤오, 출장비) 4종** → §1~§7
> - **`Daily Reimbursement`(일상 비용) 2종** → **§8**
>
> **작성**: 2026-08-18 (출장비 4종) · 2026-08-31 (Daily 2종 추가). 사용자가 사내 시스템에서 직접 받은 원본을 `openpyxl`·원본 XML로 뜯어 확인.
> **성격**: 이 문서는 **변하지 않는 사실(구조)** 만 담는다. 어떻게 채울지·왜 그렇게 채우는지는 → [expense-context.md](./expense-context.md)
> **표기**: [CLAUDE.md 「중국어를 다루는 법」](../CLAUDE.md) 규약에 따라 원문 + 한국어 + 위치를 항상 병기한다.

---

## 결론 먼저

- 템플릿은 **4개 파일**이고, 각각 **보이는 시트 1개 + 숨김 시트 2개**로 되어 있다.
- **헤더가 전부 `*`로 시작한다 = 모든 항목이 필수.** 한 칸이라도 비면 임포트가 막힐 수 있다.
- **데이터는 2행부터** 넣는다. 1행은 헤더.
- **엑셀을 새로 만들면 안 된다.** 숨김 시트에 시스템 식별 토큰이 들어 있어, 다운로드받은 파일을 열어 **행만 추가**해야 한다.
- **데이터 열이 전부 「텍스트」 서식으로 지정돼 있다** (`numFmtId=49` = 엑셀 내장 `@`). 4개 파일·모든 열 예외 없음. → §2-1

---

## 1. 파일 4종 — 무엇을 어디에 넣나

| # | 원본 파일명 | 시트명(=탭명) | 한국어 | 열 수 |
|---|---|---|---|---|
| 1 | `差旅报销_机票、火车票_*.xlsx` | `机票_火车票` | **항공권·기차표** | 8 |
| 2 | `差旅报销_酒店费用_*.xlsx` | `酒店费用` | **호텔 숙박비** | 8 |
| 3 | `差旅报销_个人餐费_*.xlsx` | `个人餐费` | **개인 식비** | 9 |
| 4 | `差旅报销_交通费_*.xlsx` | `交通费` | **시내 교통비** (택시·지하철 등) | 6 |

> **⚠️ 헷갈리기 쉬운 지점**: **기차표는 `交通费`(교통비)가 아니라 `机票、火车票`(항공권·기차표) 파일**이다. `机票、火车票` 파일 G열 `交通工具`(교통수단) 드롭다운에 `火车`(일반 기차)·`高铁`(고속철) 항목이 실제로 들어 있고, `交通费` 파일에는 그 드롭다운 자체가 없다. 잘못 넣으면 임포트는 통과해도 **엉뚱한 비용 항목으로 정산된다.**

---

## 2. 공통 구조 — 파일 4개 모두 동일

### 시트 3개
| 시트 | 상태 | 역할 |
|---|---|---|
| 항목명 시트 (`个人餐费` 등) | 보임 | **여기 2행부터 데이터를 넣는다** |
| `..._dict` (예: `个人餐费_dict`) | **숨김** | 드롭다운 선택지 원본. 이름 정의(defined name)로 참조됨 |
| `_easeflow_import_info_` | **숨김** | A1에 **파일마다 다른 16바이트 토큰**. 시스템이 "이게 어느 양식인지" 식별하는 값으로 추정 |

> 시트명 `_easeflow_import_info_`에서 이 업로드 파이프라인이 사내 워크플로 엔진 **easeflow(易流)** 임을 알 수 있다.
> 토큰 실제 값은 **의도적으로 이 문서에 적지 않는다** (레포가 PUBLIC이므로). 필요하면 원본 파일에서 직접 읽는다.

### 2-1. 셀 서식 — 데이터 열은 전부 「텍스트」다 *(2026-08-18 실측)*

`xl/styles.xml`을 열어보면 커스텀 서식(`<numFmts>`)은 **없다.** 대신 **내장 서식 `numFmtId=49`(= `@`, 텍스트)** 가 `applyNumberFormat="true"` 와 함께 데이터 열 전부에 걸려 있다. 열 → 스타일 연결은 시트의 `<cols><col style="N">` 이 담당한다.

| 파일 | 데이터 열 | `cellXfs` 인덱스 | `numFmtId` |
|---|---|---|---|
| `机票、火车票` | A~H (8열) | 1~8 | **49 = 텍스트** |
| `酒店费用` | A~H (8열) | 1~8 | **49 = 텍스트** |
| `个人餐费` | A~I (9열) | 1~9 | **49 = 텍스트** |
| `交通费` | A~F (6열) | 1~6 | **49 = 텍스트** |

헤더 1행만 `numFmtId=0` + 분홍 채움(`fillId=3`)으로 따로 잡혀 있다.

**함의**: 날짜를 엑셀 날짜값(시리얼 넘버)으로 넣으면 텍스트 서식 열이라 `46252` 같은 숫자로 보인다. **양식 제작자가 문자열 입력을 전제했다는 증거**다. 어떤 문자열 포맷으로 쓸지는 → [expense-context.md](./expense-context.md)

재현:
```bash
unzip -p 差旅报销_个人餐费_*.xlsx xl/styles.xml | grep -o '<cellXfs.*</cellXfs>'
unzip -p 差旅报销_个人餐费_*.xlsx xl/worksheets/sheet1.xml | grep -o '<cols>.*</cols>'
```

### 그래서 지켜야 할 것
1. **새 워크북을 만들지 않는다.** 원본을 복사해서 그 복사본에 행을 추가한다.
2. **숨김 시트를 지우거나 건드리지 않는다.** `openpyxl`로 열고 저장할 때 숨김 시트가 보존되는지 확인할 것.
3. **파일명은 바꿔도 된다** — 시스템이 읽는 건 파일 내용이지 파일명이 아니다. 다만 서버가 파일명까지 검사하는지는 **미확인**이므로, 원본 접두어(`差旅报销_机票、火车票_`)는 남기고 뒤만 바꾸는 것을 권장.

### 데이터 유효성 검사(드롭다운) 적용 범위
드롭다운이 걸린 열은 **2행 ~ 65534행**에 적용돼 있다. 목록에 없는 값을 넣으면 엑셀이 `此值与单元格定义格式不一致！`(이 값은 셀에 정의된 형식과 맞지 않습니다) 오류를 낸다.

---

## 3. 파일별 컬럼 — 전체

### 3-1. `机票、火车票` (항공권·기차표) — 8열

| 열 | 원문 | 한국어 | 입력값 |
|---|---|---|---|
| A | `*发生日期` | 발생일자 | 날짜 |
| B | `*币种` | 통화 | 통화 코드 (→ §4) |
| C | `*本次支付金额(原币)` | 이번 지급액(원래 통화 기준) | 숫자 |
| D | `*是否国际/港澳台出差` | 국제/홍콩·마카오·대만 출장 여부 | **드롭다운: `是`(예) / `否`(아니오)** |
| E | `*出发地点` | 출발지 | 자유 입력 |
| F | `*抵达地点` | 도착지 | 자유 입력 |
| G | `*交通工具` | 교통수단 | **드롭다운 6종 (→ 아래)** |
| H | `*详细说明` | 상세 설명 | 자유 입력 |

**G열 `交通工具`(교통수단) 드롭다운 — 6개 전부:**

| 원문 | 한국어 |
|---|---|
| `飞机经济舱` | 비행기 이코노미석 |
| `火车` | 일반 기차 |
| `飞机公务/头等舱` | 비행기 비즈니스/퍼스트석 |
| `高铁一等座` | 고속철(가오톄) 1등석 |
| `高铁二等座` | 고속철 2등석 |
| `高铁商务座` | 고속철 비즈니스석 |

### 3-2. `酒店费用` (호텔 숙박비) — 8열

| 열 | 원문 | 한국어 | 입력값 |
|---|---|---|---|
| A | `*入住日期` | 체크인 날짜 | 날짜 |
| B | `*离店日期` | 체크아웃 날짜 | 날짜 |
| C | `*币种` | 통화 | 통화 코드 |
| D | `*本次支付金额(原币)` | 이번 지급액(원래 통화) | 숫자 |
| E | `*是否国际/港澳台出差` | 국제/홍마대 출장 여부 | **드롭다운: `是` / `否`** |
| F | `*住宿人数` | 숙박 인원 수 | 숫자 |
| G | `*同住人` | 같이 묵은 사람 | 자유 입력 |
| H | `*详细说明` | 상세 설명 | 자유 입력 |

> **주의**: 이 파일만 A·B열이 **둘 다 날짜**다(체크인·체크아웃). 다른 파일의 A열 `发生日期`와 헷갈리지 말 것.

### 3-3. `个人餐费` (개인 식비) — 9열 (가장 많음)

| 열 | 원문 | 한국어 | 입력값 |
|---|---|---|---|
| A | `*发生日期` | 발생일자 | 날짜 |
| B | `*币种` | 통화 | 통화 코드 |
| C | `*本次支付金额(原币)` | 이번 지급액(원래 통화) | 숫자 |
| D | `*是否国际/港澳台出差` | 국제/홍마대 출장 여부 | **드롭다운: `是` / `否`** |
| E | `*国际出差地区` | 국제 출장 지역 | 자유 입력 (드롭다운 없음) |
| F | `*就餐人数` | 식사 인원 수 | 숫자 |
| G | `*就餐人` | 같이 식사한 사람 | 자유 입력 |
| H | `*就餐天数` | 식사 일수 | 숫자 |
| I | `*详细说明` | 상세 설명 | 자유 입력 |

> **⚠️ `国际出差地区`(국제 출장 지역)는 자유 입력이 아니라 「마스터 목록 조회 칸」이다** *(2026-08-31 실측 — §7)*. 엑셀엔 드롭다운이 없지만 **서버가 사내 마스터 목록과 대조**한다. 목록에 없는 값은 `字段[国际出差地区]的值[X]在数据源中不存在`(필드 `国际出差地区`의 값 X가 데이터 소스에 존재하지 않습니다)로 튕긴다. 4개 파일 중 이 칸은 여기에만 있다.
> **`就餐天数`(식사 일수)가 있다는 건** 며칠치를 한 행에 묶을 수 있는 설계라는 뜻일 수 있다. 이것도 미확정.

### 3-4. `交通费` (시내 교통비) — 6열 (가장 단순)

| 열 | 원문 | 한국어 | 입력값 |
|---|---|---|---|
| A | `*发生日期` | 발생일자 | 날짜 |
| B | `*币种` | 통화 | 통화 코드 |
| C | `*本次支付金额(原币)` | 이번 지급액(원래 통화) | 숫자 |
| D | `*出发地点` | 출발지 | 자유 입력 |
| E | `*抵达地点` | 도착지 | 자유 입력 |
| F | `*详细说明` | 상세 설명 | 자유 입력 |

> **4개 중 유일하게 드롭다운이 하나도 없다.** 숨김 시트 `交通费_dict`도 비어 있다.
> **그런데 화면 폼에는 `*是否国际/港澳台出差`가 필수로 있다** — 엑셀 템플릿에는 그 열이 없다. §5 참고.

---

## 4. `币种`(통화) 값 — 화면 드롭다운 실측

엑셀 템플릿의 B열(`酒店费用`은 C열) `币种`에는 드롭다운이 없어 자유 입력이지만, **화면 폼의 드롭다운 표기는 `코드-중국어명` 형식**이다. 실측한 목록:

| 값 (그대로 입력) | 한국어 |
|---|---|
| `CNY-人民币` | 중국 위안 |
| `HKD-港币` | 홍콩 달러 |
| `USD-美元` | 미국 달러 |
| `EUR-欧元` | 유로 |
| `GBP-英镑` | 영국 파운드 |
| `KRW-韩元` | **한국 원** |
| `JPY-日元` | 일본 엔 |
| `AUD-澳大利亚元` | 호주 달러 |
| `CAD-加拿大元` | 캐나다 달러 |
| `TWD-新台币` | 대만 달러 *(화면에서 잘려 부분 확인)* |

> 목록은 스크롤이 더 남아 있어 **위가 전부는 아니다.** 필요한 통화가 없으면 화면에서 다시 확인할 것.
> **엑셀에 `CNY-人民币` 전체를 넣을지 `CNY`만 넣을지는 미확정** — 첫 업로드로 검증한다.

---

## 5. 화면 폼 vs 엑셀 템플릿 — 덮는 범위가 다르다

NetEase OA 화면(`报销项目明细` 정산 항목 상세)의 `交通费` 탭 컬럼과 엑셀 템플릿을 대조한 결과:

| 화면 컬럼 | 한국어 | 엑셀에 있나 |
|---|---|---|
| `*发生日期` `*币种` `*本次支付金额(原币)` `*出发地点` `*抵达地点` `*详细说明` | (위 §3-4와 동일) | ✅ 있음 |
| **`*是否国际/港澳台出差`** | 국제/홍마대 출장 여부 | ❌ **없음** (화면에선 필수) |
| `发票` | 인보이스(파피아오) 첨부 | ❌ 화면에서 `+` 버튼으로 업로드 |
| `行程单` | 여정표 첨부 | ❌ 화면 업로드 |
| `费用相关证明` | 비용 관련 증빙 첨부 | ❌ 화면 업로드 |
| `兑付款币种汇率` | 지급 통화 환율 (기본 `1.0000`) | ❌ 시스템 자동 |
| `本次支付金额` | 이번 지급액(위안 환산) | ❌ 시스템 자동 계산 |

**함의 2개**
1. **`交通费`의 국제출장 여부는 엑셀로 못 채운다.** 임포트 후 화면에서 채워야 할 가능성이 크다 → 첫 업로드 때 확인.
2. **영수증 파일 첨부는 엑셀 임포트 범위 밖이다.** 엑셀은 텍스트 값만 채운다.

### 화면 구조 (참고)
- 정산서 제목 형식: `差旅报销-{신청자 영문 이름}-{날짜}` — 시스템이 자동 생성한다 *(실명 예시는 이 레포가 PUBLIC이라 싣지 않는다)*
- `报销项目明细`(정산 항목 상세) 안에 **탭 4개가 공존**: `机票、火车票` / `酒店费用` / `个人餐费` / `交通费` → **정산서 1건에 4개 파일이 모두 들어간다.**
- 탭 옆에 `业务招待费`(업무 접대비) `团建费`(팀빌딩비) 추가 버튼이 따로 있다 → **다른 비용 유형은 별도 템플릿**일 것.
- 엑셀 업로드 진입점 = **`Bulk Import`** 버튼. 옆에 `批量上传发票`(인보이스 일괄 업로드), `Add details`(행 직접 추가).
- `报销汇总`(정산 요약)의 `申请付款总金额`·`本次支付总金额`은 **자동 합계**.

---

## 6. 재현 방법 — 구조를 다시 확인하고 싶을 때

템플릿을 새로 받았을 때 이 문서와 달라진 게 없는지 확인하는 스크립트:

```python
import glob, openpyxl
for path in sorted(glob.glob('差旅报销_*.xlsx')):
    wb = openpyxl.load_workbook(path)
    print(path, [(ws.title, ws.sheet_state) for ws in wb.worksheets])
    ws = wb.worksheets[0]
    print("  헤더:", [c.value for c in ws[1]])
    for dv in ws.data_validations.dataValidation:
        print("  드롭다운:", dv.sqref, "→", dv.formula1)
    for n in wb.defined_names:                      # 드롭다운 선택지 실제 값
        ref = wb.defined_names[n].value
        print("  ", n, ref)
```

> 파일명을 `inspect.py`로 저장하면 파이썬 표준 모듈 `inspect`와 충돌해 실행이 깨진다. 다른 이름을 쓸 것.

---

## 7. 서버 실측 갱신 *(2026-08-31 — 업로드 오류 로그 4장 + 화면 캡쳐)*

`Downloads`에 남아 있던 **서버 오류 로그 4장**(8/18 타이베이 1장, 8/24 옌타이 3장)을 뜯어 대조한 결과다. 오류 로그는 업로드한 파일에 **J열을 덧붙여 되돌려주는** 형태라, 그 자체로 「무엇이 통과하고 무엇이 튕기는가」의 정답지다.

### 7-1. `国际出差地区`(국제 출장 지역)은 마스터 목록 조회 칸

| 넣은 값 | 서버 응답 |
|---|---|
| `韩国` (한국) | ✅ **통과** |
| `中国` (중국) | ❌ `在数据源中不存在` (데이터 소스에 없음) |
| `烟台市` (옌타이시 — 도시명) | ❌ `在数据源中不存在` |

**화면 드롭다운에서 실제로 확인한 값 10개** (2026-08-31, 캡쳐 2장. 목록이 더 남아 있어 **전체는 아니다**):

| 순서 | 원문 | 한국어 |
|---|---|---|
| **1** | **`韩国`** | **한국** ← 목록 맨 위. 오류 로그 통과 실증 |
| 2 | `挪威` | 노르웨이 |
| 3 | `瑞士` | 스위스 |
| 4 | `冰岛` | 아이슬란드 |
| 5 | `迪拜` | 두바이 |
| ? | `澳门` | 마카오 |
| ? | `台湾` | 대만 |
| ? | `南非` | 남아프리카공화국 |
| ? | `利比亚` | 리비아 |
| ? | `安哥拉` | 앙골라 |

- **가나다·알파벳 순이 아니다.** `韩国`이 1번인 걸 보면 사용 빈도나 사내 마스터 등록 순으로 보인다.
- **이 칸은 검색이 되는 드롭다운**이다(돋보기 아이콘). 원하는 지역이 목록에 있는지는 한자를 쳐서 필터하면 바로 확인된다.
- ⚠️ **`香港`(홍콩)은 아직 미확인.** 미처리 상태인 `HKF-202608`(홍콩) 건을 정산할 때 필요하다 — 그때 `香`을 쳐서 확인한다.
- ⚠️ **`中国`은 여전히 없다.** 두 캡쳐 어디에도 보이지 않고, 서버도 거부했다.

- **전부 중국 밖 지역이다.** 이 칸의 이름 그대로 **「해외 출장 지역」** 이라 `中国`이 목록에 있을 수 없다.
- 도시 단위(`烟台市`)도 없다. **국가·지역 단위**다.
- `韩国`은 오류 로그로 통과가 확인됐고, `台湾`은 화면 목록에서 확인됐다.
- ⚠️ **전체 목록은 아직 못 받았다.** 필요한 지역이 목록에 있는지는 화면에서 확인해야 한다.

### 7-2. 헤더에 `*`가 붙어도 실제로는 필수가 아닌 칸이 있다

| 칸 | 비웠을 때 |
|---|---|
| `就餐人`(같이 식사한 사람) — `个人餐费` G열 | ✅ 오류 없음. **비우는 게 정답** |
| `是否国际/港澳台出差` — `个人餐费` D열 | ✅ 오류 없음 |

**판단 근거**: 8/24 로그에서 D열을 비웠는데 **D보다 뒤에 있는 E열 오류**가 보고됐다. 앞 칸이 문제였다면 그게 먼저 잡혔을 것이다.

반대로 `就餐人`에 **이름 문자열**을 넣으면 `字段[就餐人]的人员[1]不存在`(필드 `就餐人`의 인원 [1]이 존재하지 않습니다)로 튕긴다. 화면에 **사람 검색 위젯**이 있어 거기서 골라야 하는 칸이다 (칩 형태로 붙는다).

### 7-3. ⚠️ **Excel로 열어서 저장하면 임포트 토큰이 사라진다** — 가장 중요

`_easeflow_import_info_`(숨김 시트) A1의 토큰이 **Excel 재저장 후 통째로 증발**했다. 같은 파일의 두 버전을 비교한 실측:

```
원본            <sheetData><row r="1"><c r="A1" t="inlineStr"><is><t>…24자…</t></is></c></row></sheetData>
Excel 재저장본  <sheetData/>          ← 비어 있음
```

- 타이베이 산출물 **3개 파일 전부** 같은 증상. 데이터 행은 멀쩡한데 토큰만 없어졌다.
- ⚠️ **반례 1건** *(같은 날 발견)*: 옌타이 `个人餐费`(8/24 다운로드본)는 Excel에서 편집·저장된 뒤에도 **토큰이 살아 있었다.** 차이는 §7-4의 **저장 형태** — 소실된 3개는 `inlineStr`, 생존한 1개는 `sharedString`이었다. **Excel이 숨김 시트의 inline string을 버린다**는 가설이 서지만 표본이 각 1건씩이라 확정이 아니다. 운영 규칙(안 연다)은 그대로 유지한다.
- **운영 규칙**: **완성된 정산 엑셀을 Excel로 열지 않는다.** 확인이 필요하면 읽기 전용으로 열고 **절대 저장하지 않는다.** 실수로 저장했으면 **백지 템플릿에 행을 다시 주입**해서 새로 만든다.
- `tools/xlsx-addrows.ps1`·`xlsx-clearrows.ps1`는 zip을 직접 조작해 이 문제가 없다 (`xlsx-clearrows.ps1` 주석에 "sharedStrings is left untouched on purpose" 로 명시돼 있다).

### 7-4. 토큰 저장 방식이 두 가지다 — 추출 코드를 짤 때 주의

| 방식 | 어느 파일에서 봤나 |
|---|---|
| **inlineStr** — `<c r="A1" t="inlineStr"><is><t>토큰</t></is></c>` | 2026-08-18 다운로드본 |
| **sharedString** — `<c r="A1" t="s"><v>인덱스</v></c>` + `sharedStrings.xml` 참조 | 2026-08-24 다운로드본 |

또한 **시트명 ↔ `sheetN.xml` 파일명이 고정이 아니다.** `_easeflow_import_info_`가 항상 `sheet3.xml`인 것도 아니다. 반드시 `workbook.xml`의 `r:id` → `xl/_rels/workbook.xml.rels`의 `Target` 순으로 해석해야 한다. (하드코딩했다가 멀쩡한 파일을 「손상」으로 오판한 적이 있다.)

### 7-5. 토큰이 「다운로드마다 새로 발급」되는지는 여전히 미확정

보유한 8개 파일의 토큰 지문을 전수 비교한 결과:

| 템플릿 | 관측 |
|---|---|
| `个人餐费` | 8/18본과 8/24본이 **다름** — 단, 두 건은 **서로 다른 정산서(`报销单`)에서 받은 것**이다 |
| `交通费`·`机票、火车票`·`酒店费用` | 8/18에 한 번만 받았고 그 뒤 계속 복사해 씀 → **비교 표본 자체가 없다** |

즉 D14의 근거는 `个人餐费` 한 쌍뿐이다. **「다운로드마다 랜덤」인지 「정산서마다 고유」인지 가릴 수 없다.** 후자라면 재사용 시 엉뚱한 정산서에 행이 들어갈 수 있다 — 첫 업로드로 판별한다.

---

## 8. `Daily Reimbursement` 양식 2종 — 출장이 아닌 상시 근무 비용 *(2026-08-31 실측)*

`差旅报销`(출장비 정산)과 **다른 계열의 정산서**다. 사용자가 사내 시스템에서 받아온 원본 2개를 뜯어 확인했다.

### 8-1. 결론 먼저 — 差旅报销과 뭐가 다른가

| | `差旅报销` (출장비) | **`Daily Reimbursement`** (일상) |
|---|---|---|
| 파일 수 | 4종 | **2종** |
| 헤더 언어 | 중국어 (`*发生日期`) | **영어** (`*Expense Date`) |
| 드롭다운 | `是/否`·`交通工具` 등 있음 | **하나도 없다** (`_dict` 시트가 비어 있음) |
| `是否国际/港澳台出差` | 있음 | **없음** — 값이 늘 같아서 물을 필요가 없다 (아래 ⚠️) |
| 사람 이름 칸 | `就餐人`·`同住人` 있음 | **없음** |
| 나머지 | 3시트(보임+`_dict`+`_easeflow_import_info_`) · 데이터 열 전부 텍스트(`numFmtId=49`) · 2행부터 · 헤더 전부 `*` · 토큰 24자 | **전부 동일** |

**같은 easeflow 파이프라인이다.** 그래서 §7-3(Excel 재저장 주의)·§7-4(토큰 추출 시 rels 경유)·2행부터 입력 같은 규칙이 그대로 적용된다.

> **⚠️ `是否国际`(국제출장 여부) 열이 없는 이유는 「국내라서」가 아니다** *(2026-08-31 사용자 정정)*.
> 사용자는 **한국에 거주·근무**한다. 회사는 **중국 법인**이다. 그래서 Daily 비용은 예외 없이 **한국에서 발생 = 회사 기준 「해외」** 다 — 값이 전 행에서 늘 같으니 열로 물을 이유가 없다.
> **「Daily = 국내」로 읽으면 안 된다.** 화면 폼에 국제출장 여부·지역을 묻는 칸이 나오면 **`是`(예) + `韩国`(한국)** 이다 (D17과 같은 논리).

### 8-2. `Local_transportation` (교통비) — 6열

| 열 | 헤더 | 한국어 | 입력값 |
|---|---|---|---|
| A | `*Expense Date` | 발생일자 | `YYYY-MM-DD` |
| B | `*Departure` | 출발지 | 자유 입력 |
| C | `*Destination` | 도착지 | 자유 입력 |
| **D** | `*Reimbursement Amount (Original Currency)` | **정산 금액(원래 통화)** | 숫자 문자열 |
| **E** | `*Currency` | **통화** | → §8-4 |
| F | `*Description` | 상세 설명 | 자유 입력 |

시트명 `Local_transportation` · 숨김 `Local_transportation_dict`(비어 있음)

### 8-3. `Consumables` (식비) — 4열

| 열 | 헤더 | 한국어 | 입력값 |
|---|---|---|---|
| A | `*Expense Date` | 발생일자 | `YYYY-MM-DD` |
| **B** | `*Currency` | **통화** | → §8-4 |
| **C** | `*Reimbursement Amount (Original Currency)` | **정산 금액(원래 통화)** | 숫자 문자열 |
| D | `*Description` | 상세 설명 | 자유 입력 |

시트명 `Consumables` · 숨김 `Consumables_dict`(비어 있음)

> **⚠️ 이름이 `Consumables`(소모품)인데 식비로 쓴다.** 사용자 지정(2026-08-31): *"하나는 교통비, 하나는 식비야."* 양식 이름만 보고 물품 구매용으로 착각하지 말 것.

### 8-4. ⚠️ 두 양식의 **금액·통화 열 순서가 서로 반대다**

```
Local_transportation :  … │ D=금액 │ E=통화 │ …
Consumables          :  … │ B=통화 │ C=금액 │ …
```

**같은 순서라고 가정하고 코드를 짜면 금액과 통화가 뒤바뀐다.** 열 순서를 파일마다 확인한다.

### 8-5. 🔴 미검증 — `Currency`(통화) 값 형식

드롭다운이 없어 자유 입력이고, **화면 표기를 아직 확인하지 못했다.**

- `差旅报销` 쪽은 `KRW-韩元`(코드+중국어명) 전체 문자열이 서버 통과 실증됐다(D15).
- 하지만 **이 양식은 헤더가 영어**라 화면도 `KRW-Korean Won` 이거나 `KRW` 단독일 수 있다.
- **현재 산출물은 `KRW-韩元`로 넣어뒀다** — 같은 easeflow 백엔드라 통화 마스터를 공유할 가능성이 높다는 판단. **첫 업로드로 판별한다.**
- → 화면에서 `Currency` 칸을 눌러 표기를 확인하면 즉시 종결된다.
